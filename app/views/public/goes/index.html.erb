<div class="container">
	<div class="row">
		<div class="container">
			<div class="row">
				<div class="col-md-6 col-md-offset-3">
		<% @goplaces.each do |place| %>
		 <%= link_to place_path(place.id) do %>
		   <%= attachment_image_tag place, :place_image %>
		 <% end %>
		</div>
	</div>
</div>

		 <%= truncate(place.place_name, length:20) %>
		<% end %>
	</div>
</div>